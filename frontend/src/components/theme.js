// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff6542',
    },
    secondary: {
      main: '#ff6542',
    },
  },
});

export default theme;
